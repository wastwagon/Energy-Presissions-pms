# Optional: use Render's database from local (Phase 2a)
# Copy this file to .env and set DATABASE_URL to your Render External Database URL.
# Leave DATABASE_URL empty or omit to use local Postgres (Phase 1).
# DATABASE_URL=postgresql://user:password@host.oregon-postgres.render.com/dbname?sslmode=require

# Optional: CORS origins for backend (comma-separated). On Render, energyprecisions.com is allowed by default.
# CORS_ORIGINS=https://energyprecisions.com,https://www.energyprecisions.com
